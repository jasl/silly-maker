{
  "name": "@sillymaker/story-silly-os",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "SillyOS 98: a retro desktop shell example (windows, taskbar, Minesweeper, Notepad, browser) built entirely on the SillyMaker engine.",
  "exports": {
    ".": "./src/story.ts"
  },
  "scripts": {
    "dev": "deno run -A npm:vite",
    "build": "deno task build:web",
    "build:web": "deno run -A npm:vite build",
    "build:desktop": "deno run -A ./tools/story.mts desktop .",
    "deploy:cf": "deno task build && deno run -A npm:wrangler deploy",
    "preview": "deno run -A npm:vite preview",
    "clean": "rm -rf dist-web dist-desktop",
    "story": "deno run -A ./tools/story.mts",
    "test": "deno run -A npm:vitest run examples/silly-os/src"
  },
  "dependencies": {
    "@sillymaker/base": "workspace:*",
    "@sillymaker/ui": "workspace:*",
    "@sillymaker/web": "workspace:*",
    "react": "19.2.7",
    "react-dom": "19.2.7",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@sillymaker/tooling": "workspace:*",
    "@types/node": "24.13.3",
    "@types/react": "19.2.17",
    "wrangler": "4.114.0"
  }
}
